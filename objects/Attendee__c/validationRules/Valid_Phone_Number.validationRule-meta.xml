<?xml version="1.0" encoding="UTF-8"?>
<ValidationRule xmlns="http://soap.sforce.com/2006/04/metadata">
    <fullName>Valid_Phone_Number</fullName>
    <active>true</active>
    <description>Ensures the phone number contains exactly 10 numeric digits when populated.</description>
    <errorConditionFormula>AND(
  NOT(ISBLANK(Phone__c)),
  NOT(REGEX(Phone__c, &quot;^[0-9]{10}$&quot;))
)</errorConditionFormula>
    <errorMessage>Phone number must contain exactly 10 digits.</errorMessage>
</ValidationRule>
